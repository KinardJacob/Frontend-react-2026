const ClientGallery = () => {
    return (
        <main className="page-placeholder">
            <section className="page-placeholder-panel">
                <h2>Client Gallery</h2>
                <p>This page will be rebuilt later. The navigation route is here now so the menu behaves cleanly.</p>
            </section>
        </main>
    );
};

export default ClientGallery;
